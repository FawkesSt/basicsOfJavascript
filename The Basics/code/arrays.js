const users = ["Matheus", "Lê", "Joe", "Jammes"];

console.log(users);

// to access a value inside an array we can use:
users[0]; // We pass the position of the value that we want, for example 0. This will return the first value of the array.

console.log(users[1] + " I lov u");

// Array is a object as well, so we can use some methods available.

console.log(users.length); // This method for example brings us the size of the array

console.log(users.splice(4,4));

